apiVersion: v1
kind: Service
metadata:
  name: {{ include "charts.fullname" . }}-headless
  labels:
    app: {{ include "charts.fullname" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      name: mysql
  clusterIP: None
  selector:
    app: {{ include "charts.fullname" . }}